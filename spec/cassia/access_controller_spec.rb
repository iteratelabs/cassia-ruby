require 'spec_helper'

RSpec.describe Cassia::AccessController do
  describe "#get_token" do
    context "when successful" do
      it "returns true"

      it "sets the access_token"
    end

    context "when unsuccessful" do
      it "returns false"

      it "sets the error and error_description values"
    end
  end
end
